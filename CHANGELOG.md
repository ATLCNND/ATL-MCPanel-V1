# 更新日志

本文件记录所有值得使用者注意的变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **容器化隔离（可选，按实例开关）**：实例可运行在 docker 容器里 ——
  独立网络（连不到节点上的其它服务、也连不到别的实例）、只读根文件系统、
  丢弃全部 Linux 权限、端口只发布到本机回环（frpc 照旧在节点上转发）。
  在「启动」页可以开关，面板会显示当前到底有没有隔离；节点缺 docker 或
  基础镜像未导入时会明确说明原因，而不是让开关看着能点却无效。
  基础镜像（约 44MB）随部署包分发，节点侧 `docker load` 即可，**不需要出网**。
  详见 `docs/CONTAINERIZATION.md`。
- **容器化开关只能由总管理员控制**（协作者/实例所有者只能看到状态）。
  这一条是刻意的：隔离限制的正是实例里的进程，而实例所有者能在启动脚本、
  控制台、插件里执行任意命令 —— 如果他自己能关掉隔离，隔离就只是一句承诺。
  实例运行中无法切换（需先停止），避免"界面显示改了、实际还在旧模式跑"。
- **穿透目标不再允许指向节点自身服务**：`local_port` 为 22/3306/7000/7400/
  8080/8443/9090/9091 等平台或数据库端口、以及全部特权端口（<1024）时会被拒绝。
  此前实例所有者可以把节点的 SSH 挂到公网（frpc 以 root 运行）。
- **第三方日志分析（LogShare.CN，默认关闭）**：控制台与玩家之间的「日志分析」页，
  列出崩溃报告与最近日志，一键上传并流式展示 AI 分析结论。
  每次上传前都会弹窗说明"内容会发送到第三方"，并提供**默认开启**的
  「过滤玩家聊天行」；必须手动勾选同意对方的服务协议与隐私政策才能继续。
  分析在服务端后台运行：中途切页/关页不会丢掉结论，同一份日志也不会重复分析。
- 「启动」页的容器化开关会同时显示节点的 Docker 版本与所用镜像。

### 安全修复（重要，建议尽快升级）

- **实例进程不再以 root 运行**（此前为严重漏洞：面板允许实例所有者编辑启动脚本、
  允许协作者发送控制台命令，而实例进程继承 Daemon 的 root 身份 ——
  于是"能管一台实例"等于"拿到节点 root"）。
  现在每台实例以**专用系统用户**运行，实例目录属主为该用户且权限 0700，
  实例之间互相读不到对方的数据。
- **被 root 信任的文件搬出实例目录**：实例元数据与 PID 记录移到
  `<state_dir>/<实例ID>/`，frpc 配置与日志移到 `<frp_state_dir>/<实例ID>/`，
  均为 root 0700。此前它们放在实例目录里，实例用户改写即可绕过资源配额、
  或把节点任意本地端口挂到自己的 frps 上。
  老装机在 Daemon 首次启动时**自动迁移**（元数据搬家 + 目录属主与权限纠正）。
- **玩家名单接口不再把输入拼进控制台命令**：玩家名里的换行会被当作第二条命令执行，
  现已在入口校验控制字符并给出明确提示。
- **登录失败不再锁死正确密码**：此前只要知道用户名，少量失败请求就能让该账号
  （包括管理员）无法登录；现在正确凭据一律放行。
- 补充安全响应头（CSP / nosniff / X-Frame-Options / Referrer-Policy；
  HSTS 仅在 HTTPS 请求上下发）。
- 节点错误信息不再向客户端泄露部署路径（细节只进服务端日志）。
- 实例所有者现在可以管理自己实例的协作者授权（此前只有总管理员可以，
  与界面提示不一致）。

### 新增

- `daemon.instance_user` 配置：实例运行身份（默认 `per-instance`，每实例一个专用
  系统用户；也支持共用一个用户名或沿用 Daemon 身份）。**以 root 运行实例不受支持**。
- 控制台支持按日志级别筛选（全部 / 信息 / 警告 / 错误），历史日志一并参与筛选。
- 玩家列表支持一键踢出 / 封禁 / 加白名单，以及设置 OP 等级（1~4 级）。
- 用户名与实例名均可修改（用户名以 UID 为稳定标识，改名不影响任何授权关联）。
- 控制台输出不再静默丢行：消费不及时会明确提示丢了多少行；
  行内高亮不再因半行拆分而错位。

### 修复

- 实例日志尾部读取不再把"半行"拆成两条消息（会影响高亮与筛选归类）。
- 控制台订阅者在取消订阅的竞态下可能触发崩溃（send on closed channel）。
- 控制台 WebSocket 代理不再泄漏协程与连接。
- 名单文件损坏时不再被当作空名单覆盖（此前会把现存名单整份清掉）。
- 调整 OP 等级不再丢失 `bypassesPlayerLimit` 等其他字段。

## 版本号约定

| 变更 | 版本号 | 升级代价 |
|---|---|---|
| 缺陷修复、小改进 | `v1.x.y` | 直接覆盖二进制即可，保留 `config.yaml` 与数据目录 |
| 新增功能 | `v1.x.0` | 同上；配置项只增不改 |
| 破坏性变更（配置格式、数据库结构、协议） | `v2.0.0` 起 | 会单独给出迁移步骤，**不保证原地升级** |

面板与节点的版本需要**匹配**：面板会向节点下发与自己版本一致的 Daemon，
跨大版本混用不受支持。
